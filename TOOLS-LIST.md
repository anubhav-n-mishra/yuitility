# YUITILITY — TOOLS LIST & DIRECTORY

A clean, comprehensive overview of all live tools on Yuitility, highlighting the newest additions built to deliver 100% client-side, privacy-first utility.

---

## ⚡ LIVE TOOLS COUNTER

```
Total Live Tools on Site: 167
All tools run 100% locally in your browser. Zero server uploads.
```

---

## 🆕 NEWLY ADDED TOOLS & DESCRIPTIONS

Below are the 32 high-demand tools recently integrated into Yuitility:

### 📝 Text & Formatting Tools
* **Case Converter (`case-converter`)**  
  Convert text instantly between UPPERCASE, lowercase, Title Case, camelCase, snake_case, and kebab-case.

* **Character Counter (`character-counter`)**  
  Count characters, words, sentences, and inspect real-time character limit progress for Twitter, LinkedIn, and Meta tags.

* **Duplicate Line Remover (`duplicate-line-remover`)**  
  Clean messy lists and datasets by stripping duplicate lines with options for case-sensitivity and trimming.

* **Text Reverser (`text-reverser`)**  
  Reverse text strings, flip word order, or generate upside-down Unicode text.

* **Text Repeater (`text-repeater`)**  
  Repeat any text string or pattern up to 10,000 times with custom delimiters.

* **Slug Generator (`slug-generator`)**  
  Transform article titles into clean, lowercase, SEO-friendly URL slugs.

* **Text Cleaner (`text-cleaner`)**  
  Sanitize raw text by stripping extra spaces, tab indentations, HTML tags, and trailing linebreaks.

* **Text to ASCII Art (`text-to-ascii-art`)**  
  Generate retro ASCII banner art from text inputs for code comments and documentation.

---

### 💻 Developer & Code Tools
* **CSS Minifier & Beautifier (`css-minifier-beautifier`)**  
  Compress CSS stylesheets to reduce bundle size or format unformatted CSS into clean, indented code.

* **JS Minifier & Beautifier (`js-minifier-beautifier`)**  
  Minify JavaScript code or format minified scripts into human-readable structures.

* **HTML Formatter (`html-formatter`)**  
  Indent and format raw HTML documents with customizable tab spacing and tag alignment.

* **JSON to CSV Converter (`json-to-csv-converter`)**  
  Flatten nested JSON arrays into downloadable CSV files directly in your browser.

* **Color Format Converter (`color-format-converter`)**  
  Convert color codes seamlessly between HEX, RGB, HSL, and HSV formats with live preview.

* **Number Base Converter (`number-base-converter`)**  
  Convert numbers between Binary (Base 2), Octal (Base 8), Decimal (Base 10), and Hexadecimal (Base 16).

* **JWT Decoder (`jwt-decoder`)**  
  Decode JSON Web Tokens safely in-browser to inspect header and payload claims without sending tokens to any server.

---

### ⏱️ Time & Productivity Tools
* **Pomodoro Timer (`pomodoro-timer`)**  
  Boost focus using customizable 25-minute work intervals, short breaks, and long breaks with browser notifications.

* **Countdown Timer (`countdown-timer`)**  
  Create live fullscreen countdown timers for events, deadlines, launches, and meetings.

* **World Clock (`world-clock`)**  
  Track current live time and time differences across major international cities and timezones.

* **Meeting Cost Calculator (`meeting-cost-calculator`)**  
  Calculate the live dollar cost per minute of corporate team meetings based on attendee count and hourly salaries.

* **Read Time Estimator (`read-time-estimator`)**  
  Calculate estimated reading time and speaking time for blog posts, scripts, and speeches based on WPM.

---

### 💰 Finance & Investment Tools
* **SIP Step-Up Calculator (`sip-step-up-calculator`)**  
  Calculate long-term mutual fund returns when increasing monthly investment contributions annually by a fixed percentage.

* **SSY Calculator (`ssy-calculator`)**  
  Estimate total maturity wealth and yearly interest accumulation for Sukanya Samriddhi Yojana savings.

* **Crypto Profit Calculator (`crypto-profit-calculator`)**  
  Calculate net crypto trading profit/loss, return percentages, and break-even sell prices factoring in exchange fees.

---

### ⚡ Health, Science & Math Tools
* **Calorie Deficit Calculator (`calorie-deficit-calculator`)**  
  Calculate target daily calorie intake and macro splits needed to reach specific weight loss goals safely.

* **Ohm's Law Calculator (`ohms-law-calculator`)**  
  Solve circuit parameters for Voltage (V), Current (I), Resistance (R), and Power (P) using interactive formulas.

* **Power Consumption Calculator (`power-consumption-calculator`)**  
  Estimate daily, monthly, and yearly electricity usage costs (kWh) for home appliances based on wattage.

* **Speed Distance Time Calculator (`speed-distance-time-calculator`)**  
  Solve speed, distance, or travel duration instantly across metric and imperial units.

* **Age in Seconds Calculator (`age-in-seconds-calculator`)**  
  Calculate exact age in total seconds, minutes, hours, days, and weeks from birth date.

---

### 🖼️ Media & Privacy Tools
* **Image Color Picker (`image-color-picker`)**  
  Sample exact HEX and RGB color codes from uploaded images using an interactive eyedropper canvas.

* **Image Flipper (`image-flipper`)**  
  Mirror images horizontally or flip them vertically instantly in your browser.

* **SVG to PNG Converter (`svg-to-png-converter`)**  
  Convert vector SVG files into high-resolution raster PNG images with custom width scaling.

* **Password Strength Checker (`password-strength-checker`)**  
  Evaluate password entropy, character diversity, and estimated crack time without transmitting text.

* **Text Encryptor (`text-encryptor`)**  
  Encrypt sensitive notes into secure Base64 ciphertext using AES-256-GCM client-side encryption.

* **File Hash Verifier (`file-hash-verifier`)**  
  Compute SHA-256, SHA-1, and MD5 file hashes locally to verify installer integrity and file checksums.

* **EXIF Data Viewer (`exif-data-viewer`)**  
  Extract camera metadata, GPS location tags, and technical image attributes from photos offline.

---

## 🌐 OVERALL SITE TOOL CATEGORIES SUMMARY

Including the newly added tools, **Yuitility** provides 167 live tools across 9 primary categories:

1. **Finance & Wealth (45 tools)** — EMI, SIP, Loans, Step-Up SIP, Crypto Profit, SSY, Salary, Tax, EPF, Taxes.
2. **Developer & Code (18 tools)** — JSON, JWT, Base Convert, CSS/JS Minifier, HTML Format, Hash, Regex.
3. **Text & Formatting (11 tools)** — Case Convert, Character Counter, Clean, Deduplicate, Slug Gen, ASCII.
4. **Time & Productivity (7 tools)** — Pomodoro, World Clock, Countdown, Meeting Cost, Read Time Estimator.
5. **PDF Tools (9 tools)** — Merge, Split, Rotate, Compress, Remove Pages, Add Numbers, Watermark.
6. **Image Tools (12 tools)** — Color Picker, Flipper, SVG to PNG, Resizer, Compress, Background Remover.
7. **Math & Science (19 tools)** — Ohm's Law, Power Consumption, Speed-Distance, Scientific Calc, Standard Dev.
8. **Health & Fitness (17 tools)** — Calorie Deficit, BMI, BMR, TDEE, Macros, Water Intake, Sleep Cycle.
9. **Construction & Everyday Utilities (29 tools)** — Room Sqft, Paint Volume, Age in Seconds, Password Check, Text Encrypt.

---

*All tools feature dynamic count integration, responsive dark UI, and 100% offline capability.*
